name: Release macOS app and Stream Deck plugin

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    name: Build and publish release
    runs-on: macos-15

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: streamdeck/package-lock.json

      - name: Run tests
        run: |
          xcodebuild test \
            -project MenubarNumbers.xcodeproj \
            -scheme MenubarNumbers \
            -destination 'platform=macOS' \
            CODE_SIGNING_ALLOWED=NO

      - name: Build unsigned app
        run: |
          xcodebuild build \
            -project MenubarNumbers.xcodeproj \
            -scheme MenubarNumbers \
            -configuration Release \
            -derivedDataPath "$RUNNER_TEMP/derived-data" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO

      - name: Package app as DMG
        id: package
        shell: bash
        run: |
          app="$RUNNER_TEMP/derived-data/Build/Products/Release/MenubarNumbers.app"
          staging="$RUNNER_TEMP/MenubarNumbers-dmg-root"
          dmg="$RUNNER_TEMP/MenubarNumbers-${GITHUB_SHA::7}.dmg"
          rm -rf "$staging"
          mkdir -p "$staging"
          ditto "$app" "$staging/MenubarNumbers.app"
          ln -s /Applications "$staging/Applications"
          hdiutil create \
            -volname "MenubarNumbers" \
            -srcfolder "$staging" \
            -ov \
            -format UDZO \
            "$dmg"
          shasum -a 256 "$dmg" | tee "$dmg.sha256"
          echo "dmg=$dmg" >> "$GITHUB_OUTPUT"

      - name: Test and package Stream Deck plugin
        id: streamdeck
        working-directory: streamdeck
        shell: bash
        run: |
          npm ci
          npm test
          npm run pack
          plugin="$PWD/dist/com.davidhermansson.menubarnumbers.streamDeckPlugin"
          shasum -a 256 "$plugin" | tee "$plugin.sha256"
          echo "plugin=$plugin" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v0.1.0-${{ github.run_number }}"
          name: "MenubarNumbers v0.1.0 build ${{ github.run_number }}"
          generate_release_notes: true
          files: |
            ${{ steps.package.outputs.dmg }}
            ${{ steps.package.outputs.dmg }}.sha256
            ${{ steps.streamdeck.outputs.plugin }}
            ${{ steps.streamdeck.outputs.plugin }}.sha256
